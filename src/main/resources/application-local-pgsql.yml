spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/featherweight?currentSchema=norpac-pgsql
    username: norpac 
    password: password

tenant: 
  name: norpac-pgsql
